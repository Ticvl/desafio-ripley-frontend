import { MontoFormatPipe } from './monto-format.pipe';

describe('MontoFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new MontoFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
