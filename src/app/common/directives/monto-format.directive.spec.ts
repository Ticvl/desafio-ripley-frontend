import { MontoFormatDirective } from './monto-format.directive';

describe('MontoFormatDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new MontoFormatDirective();
    expect(directive).toBeTruthy();
  });
});
