import { RutFormatDirective } from './rut-format.directive';

describe('RutFormatDirectiveDirective', () => {
  it('should create an instance', () => {
    //const directive = new RutFormatDirective();
    //expect(directive).toBeTruthy();
  });
});
